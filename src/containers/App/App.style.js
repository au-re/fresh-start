import styled from "styled-components";

/* styles for the App */
export default (componentName) => styled(componentName) `
  text-align: center;
  height: 100%;

  .App__Sidebar {
    height: 100%;
    background: #2D3446;
    position: absolute;
  }
`;
